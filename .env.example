# Shopify Storefront API
# Get these from your Shopify admin: Settings > Apps > Develop apps > API credentials
# Reference: https://shopify.dev/docs/api/storefront
PUBLIC_STOREFRONT_API_TOKEN=your_storefront_api_token_here
PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# Session Secret (Private)
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Use at least 32 characters for security
# Reference: https://remix.run/docs/en/main/guides/sessions
SESSION_SECRET=your_random_session_secret_here_at_least_32_chars

# Optional: Private Storefront API Token (for server-side operations)
# Only needed if using Admin API for mutations
# PRIVATE_STOREFRONT_API_TOKEN=your_private_token

# Development Notes:
# 1. Copy this file: cp .env.example .env
# 2. Fill in actual credentials from your Shopify store
# 3. Never commit .env to version control (protected by .gitignore)
# 4. For production: Use Shopify Oxygen Secrets or GitHub repository secrets

